<resources>
    <!-- Tema baseado no Material Components (necessário para BottomNavigationView) -->
    <style name="Theme.ParqueDeEstacionamento" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:statusBarColor">@color/primary</item>
    </style>

    <style name="Theme.ParqueDeEstacionamento.Main" parent="Theme.ParqueDeEstacionamento" />
</resources>
